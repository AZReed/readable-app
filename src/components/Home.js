import React, { Component } from "react";
import { Button } from "semantic-ui-react";

class Home extends Component {
  render() {
    return (
      <div>
        <Button>Ale</Button>
      </div>
    );
  }
}

export default Home;
